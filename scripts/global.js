// 方向
const TOP = 0;
const RIGHT = 90;
const BOTTOM = 180;
const LEFT = 270;

// Math函数
const abs = Math.abs;
const PI = Math.PI;
const sin = Math.sin;
const cos = Math.cos;
const random = Math.random;
const floor = Math.floor;
